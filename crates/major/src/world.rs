use crate::component::{
    archetype::{self, Archetype},
    table::Metatable,
};

pub struct World {}

impl World {
    pub fn supertype(&mut self, archetype: Archetype) -> Metatable {
        todo!()
    }
}
